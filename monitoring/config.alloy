discovery.docker "shopflow" {
  host = "unix:///var/run/docker.sock"
  filter {
    name = "label"
    values = ["com.docker.compose.project=shopflow"]
  }
}
discovery.relabel "shopflow" {
  targets = []
  rule {
    source_labels = ["__meta_docker_container_label_com_docker_compose_service"]
    target_label = "service"
  }
}
loki.source.docker "shopflow" {
  host = "unix:///var/run/docker.sock"
  targets = discovery.docker.shopflow.targets
  relabel_rules = discovery.relabel.shopflow.rules
  labels = {"project" = "shopflow"}
  forward_to = [loki.write.local.receiver]
}
loki.write "local" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}
